# Default backend install for hosted/free-tier deployment.
# This intentionally excludes the optional ML stack in requirements-ml.txt
# because torch/sentence-transformers can exceed 512 MB memory limits.

-r requirements-core.txt
